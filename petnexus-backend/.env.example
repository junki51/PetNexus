APP_ENV=development
PORT=8080

# Render/production connection string. Leave empty to use the local DB_* values.
DATABASE_URL=

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=petnexus
DB_SSLMODE=disable

JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=24h
